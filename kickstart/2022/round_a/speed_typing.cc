#include <bits/stdc++.h>
#include <iostream>
#include <string>

using namespace std;

void solve(string s, string p) {

  int n = s.size(),m = p.size(), i = 0, j = 0;


  while(i < n && j < m){


  }


  // Impossible when barbara missed some letters

}

int main() {

  ios::sync_with_stdio(0);
  cin.tie(0);

  int t;
  string i, p;

  cin >> t;

  for (int j = 1; j <= t; ++j){
    cin >> i >> p;
    cout << "Case #" << j << ": ";
    solve(i, p);
    cout << "\n";
  }






}
