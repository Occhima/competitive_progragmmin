
#include <bits/stdc++.h>
using namespace std;

void solve(int n, int k) {}

int main() {

  ios::sync_with_stdio(0);
  cin.tie(0);
  int t, k, n;

  cin >> t;
  while (t--) {

    cin >> n >> k;
    solve(n, k);
  }
}
