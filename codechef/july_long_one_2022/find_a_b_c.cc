#include <bits/stdc++.h>
using namespace std;

void solve(int arr[], int n) {


}

int main() {

  ios::sync_with_stdio(0);
  cin.tie(0);
  int t, n;

  cin >> t;

  while (t--) {
    cin >> n;
    int arr[n+1];
    for(int i = 0; i < (n + 1); i++)
      cin >> arr[i];

    solve(arr, n+1);

  }
}
